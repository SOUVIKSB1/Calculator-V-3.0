/* =========================================
   config.example.js  —  TEMPLATE (safe to commit)
   =========================================
   1. Copy this file  →  config.js
   2. Paste your real key into config.js
   3. Never commit config.js  (it's in .gitignore)

   Get a free Gemini key at:
   https://aistudio.google.com/apikey
========================================= */

const CONFIG = {
  GEMINI_API_KEY: '',   // ← your key goes here (in config.js, not here)
};
